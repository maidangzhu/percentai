{
  "name": "percent",
  "private": true,
  "version": "0.1.5",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --parallel --filter percent-server --filter percent --filter percent-cms --filter percent-landing dev",
    "client": "pnpm --filter percent dev",
    "server": "pnpm --filter percent-server dev",
    "landing": "pnpm --filter percent-landing dev",
    "cms": "pnpm --filter percent-cms dev"
  }
}

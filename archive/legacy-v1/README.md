# Percent Legacy v1 Archive

This directory contains the pre-v2 implementation.

It is kept for reference only:

- current Tauri client implementation
- old landing/CMS/server apps
- old runtime package
- old package and lock files

Do not continue feature development here. New work belongs in `v2/` and should follow the source-of-truth docs in `docs/`.

